import Layout from "../components/Layout"

export const Login = () => {
    return (
        <Layout title="Sign in to BMS">
            <div className="justify-center container mt-12 text-gray-800">
                Sign in to BMS (Now working!)
            </div>
        </Layout>
    )
}

export default Login
